# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# test / coverage
coverage/
.nyc_output/

# editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json

# supabase local
supabase/.branches/
supabase/.temp/
.supabase/

# Backups may contain PII — never commit.
backups/
*.dump
*.dump.enc
*.sql.gz
*.sql.enc

# Claude Code runtime state — session id/pid/timestamp, not governance
.claude/scheduled_tasks.lock

# Claude Code per-developer local permission overlay — never commit.
# May hold machine-specific paths or local-only allow rules; the committed
# .claude/settings.json is the shared, reviewed governance layer.
.claude/settings.local.json

# Claude Code subagent worktrees (isolated repo checkouts, auto-created)
.claude/worktrees/
