# Guardrail dry-run artifact

Temporary file created solely to validate scripts/ai-dev-merge.sh's
mechanical gates (draft-block, pending-CI-block) via a real PR. This PR is
closed without merging and the branch is deleted immediately after the test.
